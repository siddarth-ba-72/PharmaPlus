// module.exports = {
//   preset: 'ts-jest',               // Use ts-jest for TypeScript support
//   testEnvironment: 'node',         // Use Node environment for testing
//   roots: ['<rootDir>/src/test'],   // Specify that tests are inside the "src/test" folder
//   transform: {
//     '^.+\\.ts$': 'ts-jest',        // Transpile TypeScript files with ts-jest
//   },
//   testMatch: ['**/*.test.ts'],     // Only run files with the ".test.ts" extension
//   moduleFileExtensions: ['ts', 'js', 'json'],
//   collectCoverageFrom: ['src/**/*.{ts,js}'],  // Collect code coverage from source files
//   coverageDirectory: '<rootDir>/coverage',    // Save coverage reports to "coverage" folder
// };
